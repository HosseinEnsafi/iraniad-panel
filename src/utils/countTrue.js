// const string = "Science";

// function allAboutStrings(str = "") {
//   if (!(str.length >= 3)) return;

//   return [
//     str.length,
//     str.slice(0, 1),
//     str.slice(-1),
//     str.slice(
//       Math.floor(
//         (str.length / 2) % 2 !== 0 ? str.length / 2 : str.length / 2 - 1
//       ),
//       Math.ceil(
//         (str.length / 2) % 2 !== 0 ? str.length / 2 : str.length / 2 + 1
//       )
//     ),
//   ];
// }

// console.log(allAboutStrings("Hello"));
